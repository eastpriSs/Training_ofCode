$

* Задача вывести числа от 1 до 10 *

*Переменная для итерации*
!i = 0;
* Цикл - основное решение и направленность задачи *
for(i < 10){
	*Постинкремент, добавляет к i единницу*
	i++;
	*Вывод чисел*
	lout(i);
}


* Справка 

В AS инкремент только один и он выглядит так var++,
тем самым он должен выполять фунцию более удобную
и повсеместную, так что при вызове операции к var 
добавится, но не вернется значение с единницей.

*
$