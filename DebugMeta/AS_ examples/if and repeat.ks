$

get(times);

if(times > 100){
  st ( "RunTime Error" ); *Экстренный выход из скрипта*
  *
  Так же мы можем использовать cr(); - он не имеет
  каких либо аргументов. Он моментально "крашит" программу.
  *
}

* Использование "->" нужно только если в блоке
больше, чем один вызов.

// Вызов можно понять по ';' в конце строки.

  *
: -> 
  out("Hello ");
  lout("world!");
times:

$